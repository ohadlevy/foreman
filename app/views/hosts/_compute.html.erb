<%= fields_for "#{type}[compute_attributes]" do |compute| %>
  <%= render "compute_resources/vms/form/#{compute_resource.provider.downcase}", :f => compute, :compute_resource => compute_resource %>
<% end -%>
